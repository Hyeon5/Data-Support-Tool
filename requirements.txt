# AI 기반 데이터 이상치 탐지 프로그램 의존성
# 내부망(오프라인) 환경에서는 아래 패키지를 미리 받아 설치한다.
#   pip download -r requirements.txt -d ./wheels   (인터넷 되는 PC)
#   pip install --no-index --find-links=./wheels -r requirements.txt   (오프라인 PC)

PySide6>=6.5
pandas>=2.0
numpy>=1.24
scipy>=1.10
scikit-learn>=1.3
openpyxl>=3.1

# .xls(구형 엑셀) 파일을 읽으려면 xlrd 가 필요하다.
xlrd>=2.0
