# 개선 계획 — 우선순위 및 분류

> 근거: `review.md`. 각 항목 ID는 리뷰 문서와 대응된다.
> 원칙: 기존 기능 무손실 · 변경 범위 최소화 · 배포물은 단일 index.html 유지.

## 우선순위별

### Critical — 즉시 수정해야 하는 문제
현재 데이터 정합성·보안을 훼손하는 Critical 결함은 **발견되지 않음**.
(대시보드 막대 미표시 버그(BUG-4)는 발견 즉시 이번 커밋에서 수정 완료)

### Major — 반드시 개선해야 하는 문제
| ID | 내용 | 이유 |
|---|---|---|
| BUG-2 | 수치 파라미터 클램프 부재(음수 Z 임계 → 전 행 이상 판정) | 사용자 실수 1회로 결과 전체 오염 |
| BUG-5 | 취소가 실행 중 검사 내부에서 미반응(대용량 LOF 20s+) | 대용량 운영 UX의 핵심 |
| TEST-1 | 테스트 하네스 미커밋 | 향후 수정자의 회귀 검증 수단 부재 |
| UX-1 | 설정 섹션 세로 나열로 스크롤 과다 | 업무 시스템 사용성 |
| 신규 | 개인정보 비식별 처리 모듈(별첨 설계) | 공공·산단 실무 요구 |

### Minor — 품질 향상을 위한 개선
| ID | 내용 |
|---|---|
| BUG-1 | `parseFloat(x) || 기본값`의 0 무시 |
| BUG-3 | 검토 일괄 적용 시 필터 리셋 |
| BUG-6 | datetime 로컬/UTC 파싱 불일치 |
| BUG-7 | 검토 저장 키에 데이터 지문 추가 |
| PERF-1 | 히스토그램 컬럼별 숫자 배열 캐시 |
| QUAL-1 | escapeHtml 통합(+XlsxWriter는 xmlEscape로 명명 분리) |
| QUAL-3 | 튜닝 상수 TUNING 객체화 |
| QUAL-4 | XlsReader isText 데드코드 제거 |
| UX-2 | 분석 완료 시 결과로 자동 스크롤 |
| UX-3 | alert → 인라인 오류 배너 통일 |
| UX-4 | 결과 테이블 컨트롤 aria-label, 스텝 aria-current |
| UX-5/OPS-1 | 푸터 버전 표기 + CHANGELOG |
| OPS-2 | `#debug` 해시로 DEBUG 활성화 |
| 보안 잔여-1 | "검토 데이터 삭제" 버튼(localStorage 정리) |

## 실행 난이도별

### 즉시 수정 가능 (각 30분 이내, 회귀 위험 낮음)
BUG-1 · BUG-3 · BUG-7 · PERF-1 · QUAL-4 · UX-2 · UX-4 · UX-5 · OPS-1 · OPS-2 · 보안 잔여-1 · BUG-2(클램프)

### 구조 개선 필요 (설계 검토 후 진행)
- BUG-5: 취소 체크포인트를 detector 내부 청크 지점으로 확장 + 즉시 중단(terminate) 옵션
- UX-1: 설정 패널 접이식 전환(대시보드 접기 패턴 재사용)
- QUAL-1: escapeHtml 전역 통합(3곳 호출부 영향 확인 필요)
- TEST-1: tests/ 하네스 커밋 + 실행 문서화
- 신규 기능: 비식별 처리 모듈 + 탭 3개 전환에 따른 **좌측 슬림 레일 도입**(review.md §8 Case 1 전환 조건 충족)
- UX-3: alert 대체 인라인 배너(오류 표시 경로 3곳 통일)

### 장기 개선
- QUAL-2: `src/` 분할 + 단일 HTML 번들 빌드 파이프라인(배포물 불변)
- PERF-3: 고차원(>6 숫자열) 대용량 시 근사 모드 분기
- PERF-4: 행 순회 3회(중복/형식오류/numericFrame) 단일 패스 통합
- BUG-6: toDate UTC 통일(비교 검증 회귀 테스트 동반 필수)
- 시계열 특화: 산단 통계의 월별/분기 시계열 계절성 고려 증감률(전년 동월 대비) 옵션 — 도메인 요구 청취 후

## 권장 순서 (다음 스프린트)
1. Major 4건: BUG-2 → BUG-5 → UX-1 → TEST-1
2. 비식별 모듈 + 사이드바 전환 (opus-implementation-prompt.md §C·§D)
3. Minor 일괄(즉시 수정 가능 그룹)
4. 장기 항목은 별도 브랜치에서
